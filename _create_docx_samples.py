"""SUPERSEDED — this script has moved to tools/legacy/_create_docx_samples.py

This file is an inert stub. The original is preserved, unmodified, at
tools/legacy/_create_docx_samples.py.

Why it was retired
------------------
It writes sample .docx files to a hardcoded path (C:\\AI-Recruitment-System\\
samples) and overwrites whatever is there. Sample and evaluation fixtures are
becoming a curated, labelled asset (see CLAUDE.md, Phase 4.1) rather than
regenerated output.

Safe to delete this stub once the repository has been committed.
See CLAUDE.md -> "Hard rules".
"""

import sys

sys.exit(
    "\n"
    "_create_docx_samples.py is SUPERSEDED and must not be run.\n"
    "\n"
    "It overwrites files in samples/ from a hardcoded path. The original is\n"
    "preserved unmodified at\n"
    "    tools/legacy/_create_docx_samples.py\n"
    "\n"
    "See CLAUDE.md -> 'Hard rules'.\n"
)
